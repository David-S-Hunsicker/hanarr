# Copy to .env if you use secrets that shouldn't live in config.yaml.
# Loaded automatically via python-dotenv.

# Only needed if config.yaml sets llm.provider: anthropic
ANTHROPIC_API_KEY=

# Only needed if config.yaml sets reminders.email.enabled: true
SMTP_PASSWORD=
